{
  "name": "mediagrowth",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tinacms dev -c \"next dev\"",
    "build": "dotenv -e .env.local -- tinacms build && next build",
    "build:vercel": "tinacms build && next build",
    "start": "next start"
  },
  "dependencies": {
    "@tinacms/cli": "latest",
    "gray-matter": "^4.0.3",
    "next": "14.2.29",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tinacms": "latest"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "dotenv-cli": "^11.0.0",
    "typescript": "^5"
  }
}
